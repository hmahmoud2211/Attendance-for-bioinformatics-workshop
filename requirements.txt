# Core dependencies
streamlit>=1.28.0
pandas>=2.0.0
openpyxl>=3.1.0

# QR Code generation
qrcode[pil]>=7.4.0
Pillow>=10.0.0

# QR Code scanning (headless build works on Streamlit Cloud/Linux servers)
# Pin to a modern build to avoid common manylinux/NumPy ABI issues.
opencv-python-headless>=4.10.0.84

# Other utilities
# Keep NumPy <2 for maximum binary-wheel compatibility on hosted Linux.
numpy>=1.26.0,<2.0
